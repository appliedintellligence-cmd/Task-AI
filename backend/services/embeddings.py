# Embedding generation disabled — google-genai removed to stay within 512MB RAM limit.
# RAG similarity search is skipped when embedding is None.

def generate_embedding(result: dict) -> None:
    return None
